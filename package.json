{
  "name": "sanitiseai",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "typecheck": "npm --prefix frontend run typecheck",
    "test": "node --test tests/*.test.mjs",
    "benchmark": "node benchmarks/run.mjs",
    "benchmark:task": "node benchmarks/task-awareness/run.mjs",
    "benchmark:reid": "node benchmarks/reidentification/run.mjs"
  }
}
