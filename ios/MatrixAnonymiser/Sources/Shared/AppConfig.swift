import Foundation

enum AppConfig {
    static let appGroupID = "group.com.matrix.anonymiser.shared"
    static let appURLScheme = "matrixanonymiser://shared"
    static let defaultAPIBaseURL = URL(string: "https://matrix-anonymiser.netlify.app")!
}
